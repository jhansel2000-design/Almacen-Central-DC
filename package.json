{
  "name": "wms-almacen-central",
  "version": "1.0.0",
  "description": "Almacén Central DC — WMS (LAN)",
  "private": true,
  "scripts": {
    "start": "node server/lan-server.js --port 8080",
    "start:lan": "node server/lan-server.js --port 8080",
    "verify": "node scripts/verify-platform.js",
    "turnos:dev": "npm run dev --prefix turnos-qr",
    "turnos:build": "npm run build --prefix turnos-qr"
  },
  "engines": {
    "node": ">=18"
  }
}
